[project]
name = "coding-lessons"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "David Wolthuis", email = "david.h.wolthuis@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.2",
    "pandas>=3.0.5",
]

[project.scripts]
coding-lessons = "coding_lessons:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
